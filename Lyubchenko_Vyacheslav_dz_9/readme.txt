Я наверное пока не понимаю главного.
Когда надо (лучше, правильнее)
методы класса задавать значениями, а когда сразу атрибуты экземпляра через конструктор __init__
Сначала когда делал, все делал через конструктор, потом подумал, что все таки наверное через конструктор задаются атрибута объекта, а не класса как требуют условия задачи.
В результате "КАША" когда надо задавать так, а когда так.
Как по мне через конструктор задавать более универсально и проще что ли, поэтому изначально все делал через ИНИТ.

Видимо все проблемы от того, что я не понимаю как ООП будет работать в настоящих программах. Как раз это и спрашивал на вебинаре тогда.
Неужели где то так и надо будет прописывать типа a.running("", "") вызывая методы таким образом.